#!/usr/bin/env python
# coding: utf-8

import matplotlib as mpl
mpl.use('Agg')
import matplotlib.pyplot as plt
import numpy as np

# 必须配置中文字体，否则会显示成方块
# 注意所有希望图表显示的中文必须为unicode格式

font_size = 10 # 字体大小
fig_size = (8, 6) # 图表大小

names = ('x1', 'x2', '#!/usr/bin/env python
# coding: utf-8

import matplotlib as mpl
mpl.use('Agg')
import matplotlib.pyplot as plt
import numpy as np

# 必须配置中文字体，否则会显示成方块
# 注意所有希望图表显示的中文必须为unicode格式

font_size = 10 # 字体大小
fig_size = (8, 6) # 图表大小

names = ('x1', 'x2', 'x3') # 姓名
subjects = ('a1', 'a2', 'a3') # 科目
scores = ((65, 90, 75), (85, 80, 90),(75, 70, 95)) # 成绩

# 更新字体大小
mpl.rcx3') # 姓名
subjects = ('a1', 'a2', 'a3') # 科目
scores = ((65, 90, 75), (85, 80, 90),(75, 70, 95)) # 成绩

# 更新字体大小
mpl.rc